# Local data — never commit real credentials
# NOTE: data/ware.db IS committed on purpose — Render has no persistent disk,
# so the live deploy reads its starting data from this file in the repo.
data/config.json
data/*_backup_*.db

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
